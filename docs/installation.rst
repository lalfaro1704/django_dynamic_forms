============
Installation
============

At the command line::

    $ easy_install django_dynamic_forms

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_dynamic_forms
    $ pip install django_dynamic_forms
